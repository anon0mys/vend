<h1><%= @machine.location %> Vending Machine</h1>

<ul>
  <% @snacks.each do |snack| %>
    <li>
      <%= snack.name %>
      <%= "$#{'%.2f' % (snack.price.to_f / 100)}" %>
    </li>
  <% end %>
  <li><%= "Average Price: $#{'%.2f' % @average_price}" %></li>
</ul>
